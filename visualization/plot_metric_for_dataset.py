import numpy as np


def plot_metric_for_dataset(dataset: str, metric: str, metric_values: np.ndarray, algorithm_names: list):
    assert(len(algorithm_names) == metric_values.shape[0])
    
    pass
